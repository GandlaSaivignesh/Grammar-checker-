html_template = """
<!DOCTYPE html>
<html>
<head>
    <title>Grammar Checker</title>
    <style>
        body { font-family: Arial, sans-serif; padding: 30px; background-color: #f4f4f4; }
        textarea { width: 100%; height: 100px; margin-top: 10px; }
        .output { background: white; padding: 15px; border-radius: 8px; box-shadow: 0 0 10px #ccc; margin-top: 20px; }
        .tag { font-family: monospace; background: #eee; padding: 3px 6px; margin: 2px; display: inline-block; border-radius: 5px; }
        .error { color: red; margin-top: 10px; }
    </style>
</head>
<body>
    <h1>📝 Grammar Checker</h1>
    <form method="post">
        <label>Enter a sentence:</label><br>
        <textarea name="sentence" required>{{ sentence }}</textarea><br><br>
        <button type="submit">Check Grammar</button>
    </form>

    {% if tagged %}
    <div class="output">
        <h3>Original POS Tags:</h3>
        {% for word, tag in tagged %}
            <span class="tag">{{ word }} → {{ tag }}</span>
        {% endfor %}
    </div>
    {% endif %}

    {% if errors %}
    <div class="output error">
        <h3>Grammar Issues:</h3>
        <ul>
        {% for e in errors %}
            <li>{{ e }}</li>
        {% endfor %}
        </ul>
    </div>
    <div class="output">
        <h3>Corrected Sentence:</h3>
        <p>{{ corrected }}</p>
        <h3>Corrected POS Tags:</h3>
        {% for word, tag in corrected_tagged %}
            <span class="tag">{{ word }} → {{ tag }}</span>
        {% endfor %}
    </div>
    {% elif sentence %}
    <div class="output">
        <h3>✅ No grammatical errors found!</h3>
    </div>
    {% endif %}
</body>
</html>
"""
